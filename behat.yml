# behat.yml
default:
  autoload:
    - %paths.base%/tests/acceptance/
  extensions:
    Behat\MinkExtension:
      base_url: http://127.0.0.1:8080/
      sessions:
        default_session:
          selenium2:
            wd_host: http://172.25.0.1:4444/wd/hub
  suites:
    checkout:
      paths:
        - %paths.base%/tests/acceptance/features/checkout.feature
      contexts:
        - CheckoutContext
